const buttonColours = ["red", "blue", "green", "yellow"];
const gamePattern = randomChosenColour;


function nextSequence(){
    const randomChosenColour = randomNumber[buttonColours.length]
    var randomNumber = Math.floor(Math.random() * 3) + 1;


}
